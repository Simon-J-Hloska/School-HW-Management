Architektura:

   [ Browser ]
        |
        | HTTPS (nejaka identifikace uzivatele)
        |
        v
[ Laravel PHP REST API Backend ]
        |
        v
    [ MySQL ]

🌐 API návrh
Endpoint	Metoda	Popis
/api/session/start	POST	zahájení sledování
/api/session/end	POST	ukončení sledování
/api/videos	GET	seznam videí
/api/leaderboard	GET	statistiky
frontend -> start timer -> backend vrati id -> frontend pak posle pri konci session id -> backend ulozi cas na backend a posle ho zpet na frontend

DB architektura:

watch_sessions
- id
- user_name
- video_id
- start_time
- end_time
- duration_seconds

SELECT
    user_name,
    SUM(duration_seconds) AS total_time
FROM watch_sessions
GROUP BY user_name
ORDER BY total_time DESC
LIMIT 10;

sessions bez end_time > 
